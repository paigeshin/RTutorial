### Calculation with Vectors ### 

# Sum 
melons[1] + melons[2] + melons[3] + melons[4]

# Simplified 
sum(melons)

# Every element multiplied with 2 
melons*2 

melons + melons

melons / 2 

sqrt(melons)

exp(melons)

adjusted_weight = c(1.2, 1.2, 1.4, 1.4)

melons * adjusted_weight

melons[1]

adjusted_weight[1] 

1.2 * 1.2

new_melons = c(1.2, 1.4, 2.4, 3.4, 1)

# Error, because each array has different length 
new_melons * adjusted_weight