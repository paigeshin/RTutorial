# Defining product vector
vector_a = c(1,2,3,4,5)

# Defining discount vector
vector_discount = c(0.2, 0.2, 0.1, 0.05, 0.05)

# Defining final product price
vector_a * (1-vector_discount)

# This script accompanies the lecture where
# we compare R with Excel or SQL